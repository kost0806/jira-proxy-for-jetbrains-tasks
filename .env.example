# Jira Configuration - Authentication is now handled per-request
JIRA_BASE_URL=https://your-jira-instance.atlassian.net

# Proxy Server Configuration
PROXY_HOST=0.0.0.0
PROXY_PORT=8000
DEBUG=false

# Application Configuration
APP_TITLE=Jira API Proxy
APP_DESCRIPTION=Proxy server for Jira API with JetBrains IDE compatibility - supports per-request authentication
APP_VERSION=1.0.0

# Security Configuration
ALLOW_ORIGINS=["*"]

# Note: Authentication credentials are no longer stored in environment variables.
# They are now passed with each API request via Authorization headers for enhanced security.