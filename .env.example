# Jira Configuration
JIRA_BASE_URL=https://your-jira-instance.atlassian.net

# Service Account Credentials (Optional)
# If provided, all API calls will use these credentials for authentication
# If not provided, credentials from each request's Authorization header will be used
JIRA_SERVICE_USERNAME=
JIRA_SERVICE_API_TOKEN=

# Proxy Server Configuration
PROXY_HOST=0.0.0.0
PROXY_PORT=8000
DEBUG=false

# Application Configuration
APP_TITLE=Jira API Proxy
APP_DESCRIPTION=Proxy server for Jira API with JetBrains IDE compatibility - supports per-request authentication
APP_VERSION=1.0.0

# Security Configuration
ALLOW_ORIGINS=["*"]

# Note:
# - If service account credentials are set, all activities in Jira will be recorded under the service account
# - If service account credentials are not set, each user's credentials from the Authorization header will be used