# Jira Configuration
JIRA_BASE_URL=https://your-jira-instance.atlassian.net

# Service Account Credentials (for API authentication)
# These credentials are used to authenticate API calls to Jira
JIRA_SERVICE_USERNAME=service-account@example.com
JIRA_SERVICE_API_TOKEN=your-service-account-api-token

# Proxy Server Configuration
PROXY_HOST=0.0.0.0
PROXY_PORT=8000
DEBUG=false

# Application Configuration
APP_TITLE=Jira API Proxy
APP_DESCRIPTION=Proxy server for Jira API with JetBrains IDE compatibility - supports per-request authentication
APP_VERSION=1.0.0

# Security Configuration
ALLOW_ORIGINS=["*"]

# Dual Authentication Mode:
# - API calls to Jira use the service account credentials above
# - Activity tracking uses the user credentials from Authorization headers
# This allows centralized API authentication while maintaining individual user accountability