# Jira Configuration
JIRA_BASE_URL=https://your-jira-instance.atlassian.net

# Service Account Credentials (Required)
# All API calls use these credentials for authentication
# All activities in Jira will be recorded under this service account
JIRA_SERVICE_USERNAME=service-account@example.com
JIRA_SERVICE_API_TOKEN=your-service-account-api-token

# Proxy Server Configuration
PROXY_HOST=0.0.0.0
PROXY_PORT=8000
DEBUG=false

# Application Configuration
APP_TITLE=Jira API Proxy
APP_DESCRIPTION=Proxy server for Jira API with JetBrains IDE compatibility - supports per-request authentication
APP_VERSION=1.0.0

# Security Configuration
ALLOW_ORIGINS=["*"]

# Note:
# - Service account credentials are REQUIRED
# - All API requests will use the service account credentials above
# - All activities in Jira will be recorded under the service account
# - Authorization headers from IDE are ignored